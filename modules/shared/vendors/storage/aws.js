
function saveFile(directoryName, fileName) {
    return false;
}

function deleteFile(directoryName, fileName) {
    return false;
}

function getFile(directoryName, fileName) {
    return false;
}

function getDirectoryFiles(directoryName) {
    return false;
}

module.exports = {
    getFile,
    saveFile,
    deleteFile,
    getDirectoryFiles,
}